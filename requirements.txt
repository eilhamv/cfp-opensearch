# Keep the OTel SDK and exporter on the SAME version — mixed versions fail at
# import time (e.g. "No module named 'opentelemetry.sdk._shared_internal'").
opentelemetry-api==1.32.1
opentelemetry-sdk==1.32.1
opentelemetry-exporter-otlp-proto-grpc==1.32.1
# Pinned to the version verified on the EC2 lab (used for bulk load + PPL checks)
opensearch-py==3.2.0
